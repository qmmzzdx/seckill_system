[Unit]
Description=Redis instance on port {PORT}
After=network.target

[Service]
ExecStart=/usr/bin/redis-server {CONF_DIR}/redis/cluster/redis-{PORT}.conf
Restart=always
Type=forking
User=redis
Group=redis

[Install]
WantedBy=multi-user.target